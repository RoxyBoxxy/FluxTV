<file name=layout.ejs path=/Users/roxsquires/Documents/Projects/music_tv_panel_ydl/views>  <div class="text-gray-400">LIVE <span id="live-time" class="text-gray-200">--:--</span></div>
  ...
  <script>
    ...
    // const bitrateEl = document.getElementById("bitrate");
    const fpsEl = document.getElementById("fps");
    const liveEl = document.getElementById("live-time");
    ...
    ws.onmessage = (event) => {
      ...
      // bitrateEl.textContent = stats.bitrate || "--";
      if (stats.out_time) {
        liveEl.textContent = stats.out_time;
      } else {
        liveEl.textContent = "--:--";
      }
      ...
    };
    ws.onclose = () => {
      ...
      // bitrateEl.textContent = "--";
      liveEl.textContent = "--:--";
      ...
    };
  </script>
